The difference in behavior seems to now be that with `BasicTextField` users are expected to swipe over the text to scroll it to the end/start.
While previously - using an `EditText` they needed to move the cursor.
Opened a ticket @Google for getting an official response/workaround/fix.
